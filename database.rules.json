{
  "rules": {
    "presence": {
      ".read": true,
      "$uid": {
        ".write": "auth != null && auth.uid === $uid && ((!data.exists() && newData.child('uid').val() === auth.uid) || newData.val() === null)",
        ".validate": "newData.val() === null || (newData.child('uid').val() === auth.uid && newData.child('nickname').isString() && newData.child('nickname').val().length >= 1 && newData.child('nickname').val().length <= 24)"
      }
    },
    "nicknameClaims": {
      ".read": true,
      "$nicknameKey": {
        ".write": "auth != null && ((!data.exists() && newData.child('uid').val() === auth.uid) || (data.child('uid').val() === auth.uid && newData.val() === null))",
        ".validate": "newData.val() === null || (newData.child('uid').val() === auth.uid)"
      }
    }
  }
}
